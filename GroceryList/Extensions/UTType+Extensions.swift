import UniformTypeIdentifiers

extension UTType {
    static let shoppingListUI = UTType(exportedAs: "com.grocerylist.shoppinglistui")
    static let productUI = UTType(exportedAs: "com.grocerylist.productui")
}
